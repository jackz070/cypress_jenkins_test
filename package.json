{
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "set NO_COLOR=1 && cypress run --browser chrome --reporter mochawesome"
  },
  "dependencies": {
    "cypress": "^13.10.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.8.2",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
