{
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "set NO_COLOR=1 && cypress run --browser chrome --reporter mochawesome && node rename-videos.js"
  },
  "dependencies": {
    "cypress": "^13.10.0"
  },
  "devDependencies": {
    "cypress-log-to-output": "^1.1.2",
    "cypress-mochawesome-reporter": "^3.8.2",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
